a = [-5, -3, -1, 1, 3, 5]
sum_neg = 0
sum_pos = 0

do i = 1, 6
    if (a(i) < 0) then
        sum_neg = sum_neg + a(i)
    else
        sum_pos = sum_pos + a(i)
    end if
end do

print *, 'Sum negative:', sum_neg
print *, 'Sum positive:', sum_pos
