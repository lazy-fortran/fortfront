! Issue #1883: ensure intent attributes survive alongside RESULT clause
program test_intent
    implicit none
contains
    function add(a, b) result(c)
        real, intent(in) :: a, b
        real :: c
        c = a + b
    end function add
end program test_intent
